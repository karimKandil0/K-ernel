# Session 01 — 2026-04-21

## What we did
- Decided to build a Rust kernel from scratch for learning
- Planned 7 phases: boot, VGA, interrupts, memory, keyboard, scheduler, shell
- Scaffolded Obsidian vault structure

## Decisions made
- Target: x86_64 bare metal via QEMU
- Bootloader protocol: started with multiboot2, **switched to Limine** (see below)
- Start with `x86_64-unknown-none` target
- Learning-first: no code written without understanding it first
- Bump allocators first (simple), upgrade later
- `flake.nix` created with rust-overlay, pinned nightly, limine, xorriso, qemu, gdb

## Bootloader switch: multiboot2 → Limine
Originally planned multiboot2. Switched to Limine because:
- multiboot2 drops into 32-bit protected mode — would need assembly to enter 64-bit long mode before any Rust runs
- Limine boots directly into 64-bit long mode, Rust entry point runs immediately
- Limine protocol is cleaner: typed request/response structs via `limine` crate
- Multiboot2 note kept in vault, marked superseded, with reason

## What we actually built this session
- `kernel/rust-toolchain.toml` — pinned nightly-2025-03-31, target x86_64-unknown-none
- `kernel/.cargo/config.toml` — default target, linker script flag, relocation-model=static
- `kernel/Cargo.toml` — limine 0.6.3 dep, panic=abort
- `kernel/src/main.rs` — no_std, no_main, panic handler, _start with VGA write attempt
- `kernel/linker.ld` — custom linker script, higher-half address, page-aligned sections
- `kernel/limine.conf` — Limine 10.x syntax (`/ Entry`, colon-separated keys)
- `Makefile` — clean/build/iso/run targets, FAT EFI image via mtools, xorriso ISO, QEMU UEFI boot

## Debugging encountered
- Limine 10.x config syntax changed from `=` to `: ` and `[]` headers don't work, use `/ Title`
- PIE/dynamic linking caused "PHDRs with different permissions" panic — fixed with relocation-model=static
- `.got` section needed explicit placement in linker.ld to get its own page
- VGA `0xB8000` doesn't work under Limine UEFI — Limine takes over display via framebuffer

## Current state
Kernel boots. Limine loads it. `_start` runs. Black screen = `loop {}` executing correctly. VGA write silently ignored because Limine UEFI disables VGA text mode.

## Next session
- Use Limine framebuffer protocol: add `FramebufferRequest` static to main.rs, get framebuffer pointer from Limine response, write pixels directly
- Once pixels work: implement proper text rendering on framebuffer
- Then: move on to GDT + IDT

## Questions / open items
- Decide: use `x86_64` crate for IDT/GDT or implement manually?
- `.got` section still present — investigate if truly needed with relocation-model=static
